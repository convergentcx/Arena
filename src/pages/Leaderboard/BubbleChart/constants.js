export const width = 960;
export const height = 640;

export const center = { x: width / 2, y: height / 2 };

export const yearCenters = {
  1: { x: width / 4, y: height / 2 },
  5: { x: width / 2, y: height / 2 },
  10: { x: (3 / 4) * width, y: height / 2 },
};
